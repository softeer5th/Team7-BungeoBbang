export const IconLoadingBox: React.FC<{ width: string; height: string }> = ({ width, height }) => (
  <div
    style={{
      width,
      height,
      backgroundColor: '#f0f0f0',
      borderRadius: '50%',
    }}
  />
);
